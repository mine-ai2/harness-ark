# Talos skills

Checked-in skills deployed to `ARK_HOME/agents/talos/skills/` on every
deploy (see deploy/deploy-remote.sh). The sync never deletes: a skill
removed from this directory lingers on the host until removed manually.
